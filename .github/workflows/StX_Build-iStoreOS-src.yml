name: 💿 StX_Build-iStoreOS-src
on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      openwrt_board:
        description: "1、请选择设备型号"
        required: true
        default: "s905d_s905x3_s912_s922x-ct2000"
        type: choice
        options:
          - s905d_s905x3_s912_s922x-ct2000
          - a311d-oes_s922x-oes-plus_wxy-oect
          - nsy-g16-plus_nsy-g68-plus_bdy-g18-pro
          - beikeyun
          - h66k
          - h68k
          - nanopi-r5s
          - s905x3
          - s912
          - s922x
      openwrt_kernel:
        description: "2、选择内核来源 (支持直接输入链接)"
        required: true
        default: "kernel_stable"
        type: choice
        options:
          - kernel_stable
          - custom_link
      kernel_custom_url:
        description: "若选custom_link，请在此输入完整.tar.gz链接"
        required: false
        default: "https://github.com/ophub/kernel/archive/refs/tags/kernel_stable.tar.gz"

env:
  VERSION: 24.10.5

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: ⏬ 下载 rootfs.tar.gz
        run: |
          mkdir -p bin/targets/armsr/armv8
          curl -L -o bin/targets/armsr/armv8/openwrt-armvirt-64-default-rootfs.tar.gz \
            https://github.com/Kwonelee/Rootfs-Actions/releases/download/generic-rootfs/istoreos-armsr-armv8-generic-rootfs.tar.gz

      - name: 🔍 查找rootfs.tar.gz所在路径
        id: find_rootfs
        run: |
          ROOTFS_FILE=$(find bin/targets/armsr/armv8/ -type f -name "*rootfs.tar.gz" | head -n1)
          echo "file=$ROOTFS_FILE" >> $GITHUB_OUTPUT

      - name: 🧱 打包iStoreOS固件
        uses: Kwonelee/amlogic-s9xxx-openwrt@main
        with:
          openwrt_path: ${{ steps.find_rootfs.outputs.file }}
          openwrt_board: ${{ inputs.openwrt_board }}
          # --- 核心改动：如果用户选 kernel_stable，直接喂入你提供的那个链接 ---
          openwrt_kernel: ${{ inputs.openwrt_kernel == 'kernel_stable' && 'https://github.com/ophub/kernel/archive/refs/tags/kernel_stable.tar.gz' || inputs.kernel_custom_url }}
          auto_kernel: false # 使用固定链接时，建议关闭自动匹配
          kernel_repo: ophub/kernel
          kernel_usage: stable
          builder_name: kwonelee

      - name: 📜 整理固件并重命名
        run: |
          mkdir -p final_out
          if [ -d "output" ]; then
            for FILE in output/*.img.gz; do
              [ -e "$FILE" ] || continue
              FILENAME=$(basename "$FILE")
              NEW_FILENAME=$(echo "$FILENAME" | sed "s/openwrt/istoreos_${{ env.VERSION }}/g")
              mv "$FILE" "final_out/$NEW_FILENAME"
            done
          fi

      - name: 🚀 上传到 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: iStoreOS-${{ env.VERSION }}-SNAPSHOT-dhcp
          body: |
            🧠 内核来源：${{ inputs.openwrt_kernel }}
            👤 用户名：root | 密码：无
          files: |
            final_out/*.img.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
